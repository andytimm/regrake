% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_raking_formula.R
\name{rr_constraints}
\alias{rr_constraints}
\alias{rr_l2}
\alias{rr_kl}
\alias{rr_exact}
\alias{rr_mean}
\alias{rr_var}
\alias{rr_quantile}
\title{Raking Constraint Functions}
\usage{
rr_l2(x)

rr_kl(x)

rr_exact(x)

rr_mean(x)

rr_var(x)

rr_quantile(x, p)
}
\arguments{
\item{x}{Variable name (unquoted) to apply the constraint to}

\item{p}{For \code{rr_quantile}, the quantile probability (0 to 1, e.g., 0.5 for median)}
}
\value{
The input variable (these functions are markers for the formula parser)
}
\description{
These functions specify constraint types for raking formulas. They are used
within formula specifications passed to \code{\link[=regrake]{regrake()}}.
}
\details{
\itemize{
\item \code{rr_exact()}: Exact equality constraint (weighted sum equals target exactly)
\item \code{rr_l2()}: Soft L2/least squares constraint (penalizes deviation from target)
\item \code{rr_kl()}: KL divergence constraint
\item \code{rr_mean()}: Match the mean of a continuous variable (alias for rr_exact on continuous)
\item \code{rr_var()}: Match the variance of a continuous variable
\item \code{rr_quantile()}: Match a specific quantile of a continuous variable
}
}
\examples{
# Match sex proportions exactly, age proportions with soft constraint
formula <- ~ rr_exact(sex) + rr_l2(age)

# Match categorical variable and continuous mean
formula <- ~ rr_exact(region) + rr_mean(income)

# Match mean and variance of a continuous variable
formula <- ~ rr_mean(income) + rr_var(income)

# Match median income
formula <- ~ rr_quantile(income, 0.5)

}
